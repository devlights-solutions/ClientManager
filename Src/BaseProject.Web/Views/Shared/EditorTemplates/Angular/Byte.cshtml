@model byte?

@{
    ViewBag.Min = String.IsNullOrEmpty(ViewBag.Min) ? byte.MinValue : ViewBag.Min;
    ViewBag.Max = String.IsNullOrEmpty(ViewBag.Max) ? byte.MaxValue : ViewBag.Max;
}
@Html.TextBox("", Model, new
{
    @class = "form-control text-right",
    ng_model = Html.AngularBindingForModel(),
    ng_change = ViewBag.NgChange,
    ng_disabled = ViewBag.NgDisabled,
    decimals = 0,
    decimal_min = ViewBag.Min,
    decimal_max = ViewBag.Max,
    formatting = "false",
    decimals_only = ""
})
